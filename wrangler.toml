# wrangler.toml - 适用于 Cloudflare Pages 的正确格式

name = "cf-pico"
pages_build_output_dir = "dist"
compatibility_date = "2024-06-01"

[build]
command = "npm run build"

[env.production]
vars = { GITHUB_USER = "chnbsdan", GITHUB_REPO = "cf-pico" }
