# wrangler.toml - Cloudflare Pages 配置（修复版）

name = "cf-pico"
pages_build_output_dir = "dist"
node_compat = true

# 构建命令
[build]
command = "npm run build"

[env.production]
vars = { GITHUB_USER = "chnbsdan", GITHUB_REPO = "cf-pico" }
